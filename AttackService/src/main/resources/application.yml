server:
  port: 8081

grpc:
  client:
    defenseService:
      address: localhost:9091
      negotiation-type: plaintext

spring:
  application:
    name: attack-service
app:
  messaging:
    mode: grpc # grpc или rabbit